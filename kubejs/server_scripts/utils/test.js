// ItemEvents.entityInteracted('stick', event => {
//     let { player, target, level } = event
//     let blockList = FindDirectionNearestBlock(target, 5, 1, 1, (level, blockPos) => { return level.getBlockState(blockPos).tags.anyMatch(tag => { return tag.equals(POI_ENTRANCE) }) })

//     player.tell(blockList)
// })
